---
layout: {{ layout }}
title: {{ title }}
modified:
categories: {{ dir }}
excerpt:
tags: []
ads: true
image:
  feature:
  url:
  teaser:
  thumb:
  alt: {{ title }}
  title: {{ title }}
---

<img src="{{ page.image.url }}" alt="{{ page.image.alt }}" title="{{ page.image.title }}">
